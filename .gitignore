# Secrets — NEVER commit the bot token
token.txt
.env

# Cache database (runtime data, can get large — never commit)
cache.db
cache.db-journal
cache.db-wal
cache.db-shm

# Python
__pycache__/
*.pyc
*.pyo

# Runtime / local junk
*.log
bot.log
*.BACKUP*
*.ORIGINAL*
chart_*.png
